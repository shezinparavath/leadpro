import 'package:flutter/material.dart';

class EndDrawerItems extends StatelessWidget {
  const EndDrawerItems({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
